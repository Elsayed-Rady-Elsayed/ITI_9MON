#include <stdio.h>
#include <stdlib.h>

int main()
{
    int Num;
    printf("enter the number:");
    scanf("%i",&Num);
    printf("the hex value of the number is : %x",Num);
    return 0;
}
